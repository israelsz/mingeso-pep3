spring:
  application:
    name: producto-service
  datasource:
    url: jdbc:mysql://localhost:3306/mingeso
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081