server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/shaaadoow/microservices-data-store
          search-paths: config-data
  application:
    name: config-service